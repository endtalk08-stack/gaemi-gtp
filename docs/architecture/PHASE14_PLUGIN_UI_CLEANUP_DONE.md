# Phase 14 — Plugin UI / central header cleanup

- Plugin header keeps only the title `플러그인` and close control.
- Removed the secondary helper line under the plugin title.
- Removed the visible central `분석 위젯` header area and its extra divider.
- Central widget host remains available so plugin-added widgets can still render without a separate title bar.
- Removed visible `분석` wording from the plugin widget description for `왜 올랐을까?`.
- No API, DB, calculation, stock-data, or core panel behavior changes.
