# Phase 15 — Mobile rail/text cleanup

- Mobile (`max-width: 1023px`) hides the left navigation rail consistently.
- Opening the left context sidebar no longer re-displays the rail on mobile.
- Removed only the `관심종목 · 최근 본 종목` subtitle beneath `내 종목`.
- No API, DB, data flow, or panel architecture changes.
