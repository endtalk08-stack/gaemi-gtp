# gaemiGTP 패널 안정화 점검본 v6

- 메인(첫 화면)의 패널 열기/닫기 상태를 동일한 토글 로직으로 정리했습니다.
- 종목 분석으로 들어갈 때 패널이 자동으로 열리지 않도록 했습니다. 분석 화면에서도 상단 버튼으로 직접 열고 닫습니다.
- 패널 좌/우 이동 버튼은 현재 위치를 기준으로 실제 이동 대상 방향을 명시적으로 계산합니다.
- 기존 저장된 방향값과 충돌하지 않도록 패널 위치 저장 키를 v2로 분리했습니다.
- 패널 폭은 화면이 허용하는 범위에서 최대 1200px까지 유지합니다.
- 왼쪽 시장정보 영역과 본문/패널 Workspace 구조는 변경하지 않았습니다.
- 패널 콘텐츠 자체는 현재 비어 있는 `#rightPanelContent` 구조를 그대로 유지했습니다.
