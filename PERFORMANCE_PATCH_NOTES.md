# gaemiGTP 일정 표시 수정

- 고정 날짜 일정 대신 실시간 핵심 경제지표 + 주요 실적 필터 유지
- `오늘밤`은 한 줄로 표시
  - 일정 있음: `오늘밤 MM/DD(요일) HH:MM #종목 실적발표`
  - 같은 날 일정이 여러 개면 `·`로 한 줄에 연결
  - 일정 없음: `오늘밤 조용함`
- `다가오는 일정` 같은 별도 제목은 사용하지 않음
- 아래 일정은 날짜별로 묶고, 같은 날짜는 한 줄에 이어 붙이며 날짜 사이에는 한 줄을 비움
- 경제지표명은 CPI/PPI/NFP/PCE/FOMC 등 핵심 일정 기준으로 짧게 표시
- 오늘밤에 이미 표시한 날짜는 아래 목록에서 중복하지 않음
- `__pycache__`, `*.pyc` 제외
