# Rail Panels

왼쪽 세로 레일의 각 아이콘이 연결할 패널 자리.

```text
Left Rail
├── 시장정보 → Left Market Sidebar
├── 관심/최근 → Left Context Sidebar
├── 향후 사용자 기능
└── 계정/설정
```

현재 단계에서는 아이콘별 실제 동작을 구현하지 않는다.
큰 영역 간 연결 관계만 고정한다.
